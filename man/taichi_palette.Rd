% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette.R
\name{taichi_palette}
\alias{taichi_palette}
\title{Ready-made palette pairs}
\usage{
taichi_palette(name = "balanced", n = 5)
}
\arguments{
\item{name}{Name of the preset: one of \code{"default"}, \code{"balanced"},
\code{"diverging"}, \code{"viridis_pair"}, \code{"brewer_pair"}, \code{"print_safe"}.}

\item{n}{Number of colours per ramp; ramps of a fixed length are
interpolated (in Lab space) when \code{n} differs from their natural length.}
}
\value{
A list of two character vectors of hex colours, \code{yin} and \code{yang}.
}
\description{
The named palette pairs that \code{\link[=geom_taichi]{geom_taichi()}}'s \code{palette} argument accepts,
available on their own so they can be inspected, checked with
\code{\link[=taichi_check_palette]{taichi_check_palette()}}, or used with the individual fish geoms.
}
\section{The presets}{

\describe{
\item{\code{"default"}}{The package's own grey yin / seal-red yang ramps ---
the look of every ggtaichi release so far. It is \emph{not} luminance
matched (the grey ramp spans the full range, the red one does not);
run \code{taichi_check_palette()} to see by how much. Kept as the default
for continuity, not because it is the most defensible choice.}
\item{\code{"balanced"}}{Two HCL ramps from \code{\link[=taichi_palette_pair]{taichi_palette_pair()}}, matched
in luminance and chroma and separated only by hue (blue yin, brick-red
yang). The recommended choice when the two sources are directly
comparable.}
\item{\code{"diverging"}}{As \code{"balanced"}, but both ramps reach a shared
near-white light end, so the two fish read as the two arms of one
diverging scale. Use it with \code{shared_limits = TRUE}.}
\item{\code{"viridis_pair"}}{The Mako and Rocket ramps, reversed to run light
to dark. They come from the same generator and are close to luminance
matched, and each ramp on its own stays ordered under colour-vision
deficiency. The two are harder to tell apart from \emph{each other} under
red-green deficiency than \code{"balanced"} is, though --- run
\code{taichi_check_palette(palette = "viridis_pair")} and look at the
protan row before choosing it.}
\item{\code{"brewer_pair"}}{ColorBrewer's sequential Blues and Oranges.
Familiar and print-friendly; less exactly matched than
\code{"balanced"}.}
\item{\code{"print_safe"}}{A grey yin ramp and a hued yang ramp on the \emph{same}
luminance trajectory. In colour the two fish are told apart by hue; in
greyscale they collapse to the same ink, so equal values still read as
equal --- the two sources are then distinguished by their position in
the glyph (yin is the top bulb, yang the bottom). The right choice for
a journal figure that may be printed in black and white.}
}
}

\examples{
taichi_palette("balanced")
taichi_palette("print_safe", n = 3)

# every preset, measured
for (p in c("default", "balanced", "diverging", "viridis_pair",
            "brewer_pair", "print_safe")) {
  cat(p, ": max |dL| = ",
      round(taichi_check_palette(palette = p)$max_luminance_diff, 1),
      "\n", sep = "")
}
}
\seealso{
\code{\link[=taichi_palette_pair]{taichi_palette_pair()}}, \code{\link[=taichi_check_palette]{taichi_check_palette()}}
}
