% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keys.R
\name{draw_key_taichi}
\alias{draw_key_taichi}
\title{A taichi-shaped legend key}
\usage{
draw_key_taichi(data, params, size, fish = "both")
}
\arguments{
\item{data}{A one-row data frame of the key's aesthetics, supplied by
ggplot2.}

\item{params}{The layer's parameters, supplied by ggplot2. \code{eyes = TRUE} is
honoured, so a plot drawn with eyes gets keys with eyes.}

\item{size}{The key size in mm, supplied by ggplot2. Unused: the glyph is
drawn in a square viewport that fills the key, so it stays round whatever
the key's aspect ratio.}

\item{fish}{Which fish carries \code{data$fill}: \code{"yin"}, \code{"yang"}, or
\code{"both"} --- the last fills the yin fish with the key colour and the yang
fish with a pale version of it, for a decorative complete symbol.}
}
\value{
A grob.
}
\description{
The legend key glyph used by \code{\link[=geom_yin_fish]{geom_yin_fish()}} and \code{\link[=geom_yang_fish]{geom_yang_fish()}}, and
therefore by \code{\link[=geom_taichi]{geom_taichi()}}: a small taichi symbol whose relevant fish is
filled with the key's colour while the other is left as an outline, so the
key looks like the mark it describes and says which half of the glyph the
scale governs. Pass it to a layer's \code{key_glyph} argument to use it
elsewhere, or use \code{key_glyph = "rect"} for plain ggplot2 rectangles.
}
\details{
Keys only appear for discrete fills. A continuous fill is drawn by
\code{\link[ggplot2:guide_colourbar]{ggplot2::guide_colourbar()}}, which is a gradient bar rather than a set of
keys, so this glyph has no effect there.
}
\examples{
library(ggplot2)
d <- data.frame(x = c(1, 2), y = 1,
                grp = factor(c("a", "b")), value = c(1, 2))

# the default for the fish geoms
ggplot(d, aes(x, y)) +
  geom_yin_fish(aes(fill = grp))

# a full symbol, or the old rectangles
ggplot(d, aes(x, y)) +
  geom_yin_fish(aes(fill = grp), key_glyph = draw_key_taichi)
ggplot(d, aes(x, y)) +
  geom_yin_fish(aes(fill = grp), key_glyph = "rect")
}
