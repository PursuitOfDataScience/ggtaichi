% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explicit.R
\name{taichi_summary}
\alias{taichi_summary}
\title{Summarise the two sources cell by cell}
\usage{
taichi_summary(data, yin, yang, x = NULL, y = NULL)
}
\arguments{
\item{data}{A data frame, the same one passed to \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}}.}

\item{yin, yang}{Unquoted column names (or strings naming columns) for the
two sources, exactly as in \code{\link[=geom_taichi]{geom_taichi()}}.}

\item{x, y}{Optional unquoted column names identifying each cell. When
given they are carried through to the output as the first columns, which
makes the result joinable back onto the plotting data.}
}
\value{
A data frame with one row per row of \code{data}: the optional \code{x} and
\code{y} cell identifiers, \code{yin}, \code{yang}, \code{difference}, \code{ratio}, \code{log_ratio},
\code{z}, \code{dominant} and \code{rank}.
}
\description{
A taichi grid is a \emph{superposition} comparison: the two sources share one
position, which makes "are these similar?" and "which is bigger here?" easy
to see and "by how much?" impossible. \code{taichi_summary()} is the tidy answer
to the last question --- the numbers behind the glyph, one row per input
row, for the reader who needs a table rather than a picture.
}
\section{The statistics}{

\describe{
\item{\code{difference}}{\code{yin - yang}. Positive means the yin fish (the top
bulb) carries the larger value.}
\item{\code{ratio}}{\code{yin / yang}, and \code{NA} wherever either value is not
strictly positive --- a ratio of a negative or zero quantity is not a
ratio, and \code{Inf} is never returned.}
\item{\code{log_ratio}}{\code{log2(yin / yang)}, so a value of 1 means yin is twice
yang and -1 means half. Symmetric around zero, which \code{ratio} is not,
and the right choice when the two sources span orders of magnitude.}
\item{\code{z}}{The difference of the two standardised sources: each column is
centred and scaled across the whole grid, then subtracted. This is the
statistic to use when the two sources are \emph{not} in the same units,
because it asks "which source is unusually high here, relative to its
own spread?" rather than "which number is bigger?".}
\item{\code{dominant}}{Which source is larger in that cell, named after the
columns supplied, or \code{"tie"}.}
\item{\code{rank}}{The cell's rank by size of \code{abs(difference)}, 1 being the
widest gap. Cells with a missing difference get \code{NA}.}
}
}

\section{A caveat on \code{rank}}{

A grid of 96 cells is 96 implicit comparisons, and the most extreme cell in
a grid that size is frequently the most extreme \emph{noise} rather than the
largest real effect. Treat \code{rank} as a list of places to look, not as a
list of findings: taking the top-ranked cell and reporting it is the
multiple-comparisons problem in miniature. With one observation per cell
there is no per-cell test to correct with, so the honest check is on the
field as a whole.
}

\examples{
summ <- taichi_summary(cafes_tg, yin = matcha, yang = espresso,
                       x = week, y = neighbourhood)
head(summ)

# the five widest gaps -- places to look, not findings; see the caveat above
head(summ[order(summ$rank), ], 5)
}
\seealso{
\code{\link[=geom_taichi]{geom_taichi()}}'s \code{explicit} argument, which shows one of these
statistics inside the glyph, and \code{\link[=geom_taichi_diff]{geom_taichi_diff()}}, which draws it as
a diverging heatmap.
}
