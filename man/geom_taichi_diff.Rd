% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explicit.R
\name{geom_taichi_diff}
\alias{geom_taichi_diff}
\title{The difference between the two sources, as a heatmap}
\usage{
geom_taichi_diff(
  yin,
  yang,
  method = c("difference", "ratio", "log_ratio", "z"),
  palette = "diverging",
  name = NULL,
  midpoint = NULL,
  symmetric = TRUE,
  na.value = "grey90",
  ...
)
}
\arguments{
\item{yin, yang}{Unquoted column names (or strings naming columns) for the
two sources, as in \code{\link[=geom_taichi]{geom_taichi()}}.}

\item{method}{Which statistic to draw: \code{"difference"} (\code{yin - yang}, the
default), \code{"ratio"}, \code{"log_ratio"} or \code{"z"}. See \code{\link[=taichi_summary]{taichi_summary()}} for
the definitions; \code{"log_ratio"} is the better choice than \code{"ratio"}
whenever the two sources span orders of magnitude, because it is
symmetric around agreement.}

\item{palette}{The diverging colours: the name of a \code{\link[=taichi_palette]{taichi_palette()}}
preset (the yang ramp's dark end becomes the low colour, the yin ramp's
dark end the high colour, so the tile agrees with the glyph), a list with
\code{yin} and \code{yang} elements, or a character vector of exactly three
colours giving \code{low}, \code{mid} and \code{high} directly.}

\item{name}{Legend title. Defaults to a label naming the statistic and the
two columns, e.g. \code{"matcha - espresso"}.}

\item{midpoint}{The value that counts as "the sources agree" and is painted
in the mid colour. Defaults to 1 for \code{"ratio"} and 0 for every other
method.}

\item{symmetric}{If \code{TRUE} (the default) the fill limits are made
symmetric about \code{midpoint}, so the mid colour really does sit at the
centre of the legend and the two directions are coloured comparably.
Set it to \code{FALSE} to use the plain data range.}

\item{na.value}{Colour for cells whose statistic is missing --- which
includes every non-positive cell under \code{"ratio"} and \code{"log_ratio"}.}

\item{...}{Further arguments passed to \code{\link[ggplot2:geom_tile]{ggplot2::geom_tile()}}, for example
\code{width}, \code{height}, \code{colour} or \code{linewidth}.}
}
\value{
An object that, added to a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} with \code{+}, draws the
difference tiles and their diverging fill scale. It is not a plot on its
own.
}
\description{
Sometimes the right chart for "how much bigger?" is not a glyph at all but
a diverging heatmap, and a package that offers one next to its signature
mark is more useful than one that insists on the mark. \code{geom_taichi_diff()}
computes one of \code{\link[=taichi_summary]{taichi_summary()}}'s statistics per cell and draws it with
\code{\link[ggplot2:geom_tile]{ggplot2::geom_tile()}} on a diverging scale centred on "the two sources
agree".
}
\details{
It is the explicit-encoding companion to \code{\link[=geom_taichi]{geom_taichi()}}: same data, same
grid, same statistics, but the relationship itself is on the page instead
of being left to the reader's eye. Use it beside a taichi grid, not instead
of one --- the glyph shows the levels, this shows the gap.
}
\examples{
library(ggplot2)

ggplot(cafes_tg, aes(week, neighbourhood)) +
  geom_taichi_diff(yin = matcha, yang = espresso) +
  theme_taichi()

# log ratio, symmetric about "the two agree"
ggplot(cafes_tg, aes(week, neighbourhood)) +
  geom_taichi_diff(yin = matcha, yang = espresso, method = "log_ratio") +
  theme_taichi()
}
\seealso{
\code{\link[=taichi_summary]{taichi_summary()}} for the same numbers as a table, and
\code{\link[=geom_taichi]{geom_taichi()}}'s \code{explicit} argument for the in-glyph version.
}
