# ggtaichi's ggproto classes

The \[ggplot2::ggproto()\] objects powering \[geom_yin_fish()\] and
\[geom_yang_fish()\]. Exported so that extension packages can inherit
from them; most users never need to touch these.
